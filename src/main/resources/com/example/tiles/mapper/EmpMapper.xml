<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tiles.mapper.EmpMapper">

    <sql id="selectAll">
        select id, username,password,name,gender,image,job,entrydate,dept_id,create_time, update_time from emp
    </sql>

    <insert id="add">
        insert into emp (id, username,password,name,gender,image,job,entrydate,dept_id,create_time, update_time)
        values (#{id},#{username},#{password},#{name},#{gender},#{image},#{job},#{entrydate},#{deptId},#{createTime},#{updateTime})
    </insert>

    <update id="upDate">
        update emp
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="job != null">
                job = #{job},
            </if>
            <if test="entrydate != null">
                entrydate = #{entrydate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        delete from emp where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <delete id="deleteByDeptId">
        delete from emp where dept_id = #{id}
    </delete>

    <select id="list" resultType="com.example.tiles.pojo.Emp">
        <include refid="selectAll"/>
        <where>
            <if test="name !=null and name != ''">
                name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entrydate between #{begin} and #{end}
            </if>
        </where>
        order by update_time desc
        limit #{start},#{pageSize};
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(*)
        from emp;
    </select>

    <select id="getById" resultType="com.example.tiles.pojo.Emp">
        <include refid="selectAll"/>
            where id = #{id}
    </select>
    <select id="getByUsernameAndPassword" resultType="com.example.tiles.pojo.Emp">
        <include refid="selectAll"/>
        where username=#{username} and password =#{password}
    </select>

</mapper>